import 'package:flutter/material.dart';

class PokemonInfoScreen extends StatelessWidget {

  const PokemonInfoScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('PokemonInfoScreen'),
      ),
    );
  }
}