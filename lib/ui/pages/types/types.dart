import 'package:flutter/material.dart';

class TypeEffectScreen extends StatelessWidget {

  const TypeEffectScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('TypeEffectScreen'),
      ),
    );
  }
}